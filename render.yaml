services:
  - type: static
    name: amara-skincare-frontend
    env: static
    buildCommand: cd frontend && npm install && npm run build
    publishPath: frontend/build
    routes:
      - source: /api/*
        destination: https://amara-skincare-backend.onrender.com/api/*
      - source: /*
        destination: /index.html
